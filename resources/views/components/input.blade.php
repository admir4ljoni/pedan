<div class="mt-4">
    <label for="{{ $id }}"
        class="block text-md font-semibold leading-6 text-gray-900">{{ $title }}</label>
    <div class="mt-1">
        <input id="{{ $id }}" name="{{ $name }}" type="{{ $type }}"
            class="block w-full rounded-md border-0 py-2 px-3 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 sm:text-base sm:leading-6">
    </div>

</div>
